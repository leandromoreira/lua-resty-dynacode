name: CI - Lua Lint and Busted (Lua Tests)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Lint
      run: |
        docker build . --file Dockerfile --tag lua-resty-dynacode:$(date +%M)
        docker run --rm -v $(pwd):/lua -w /lua lua-resty-dynacode:$(date +%M) bash -c "luacheck -q ."
    - name: Tests
      run: |
        docker build . --file Dockerfile --tag lua-resty-dynacode:$(date +%M)
        docker run --rm -v $(pwd):/lua -w /lua lua-resty-dynacode:$(date +%M) bash -c "./resty_busted -c --shuffle"
